# test

This repository is intentionally minimal.

The current follow-up change adds a baseline project file so the branch is no
longer empty and can pass basic implementation review and validation checks.
